import React from 'react';
import { shallow } from 'enzyme';
import xxx from '.';

describe('xxx', () => {
  it('should render with defaults', () => {
    const component = shallow(<xxx />);
  });
});
