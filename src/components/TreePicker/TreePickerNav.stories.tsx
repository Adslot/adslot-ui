// this is hidden from sidebar
import type { Meta } from '@storybook/react';
import Component from './TreePickerNav';

const meta = {
  title: 'Components/TreePickerNav',
  component: Component,
} satisfies Meta<typeof Component>;

export default meta;

export const TreePickerNav = {};
