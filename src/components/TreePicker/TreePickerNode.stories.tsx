// this is hidden from sidebar
import type { Meta } from '@storybook/react';
import Component from './TreePickerNode';

const meta = {
  title: 'Components/TreePickerNode',
  component: Component,
} satisfies Meta<typeof Component>;

export default meta;

export const TreePickerNode = {};
