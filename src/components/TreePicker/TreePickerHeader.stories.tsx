// this is hidden from sidebar
import type { Meta } from '@storybook/react';
import Component from './TreePickerHeader';

const meta = {
  title: 'Components/TreePickerHeader',
  component: Component,
} satisfies Meta<typeof Component>;

export default meta;

export const TreePickerHeader = {};
