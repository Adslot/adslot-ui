import Props from '../containers/Props.jsx';
import DesignNotes from '../containers/DesignNotes.jsx';

## Textarea

```jsx live=true
<div style={{ width: 300 }}>
  <Textarea maxLength={250} />
</div>
```

<DesignNotes>
  Text area used to capture comments, creative ad tags etc. If there is a character limitation then the count should be
  listed below and on the right side of the text area. Font size is <span className="text-bold">12px</span>, weight{' '}
  <span className="text-bold">300</span>.
</DesignNotes>

<Props componentName="Textarea" />
