import Props from '../containers/Props.jsx';
import DesignNotes from '../containers/DesignNotes.jsx';

## Search List

```jsx live=true
const Example = () => <SearchList />;

render(Example);
```

<DesignNotes>
  Search list is an list with selectable options. You can also wire up a search component to filter through your async
  list.
</DesignNotes>

<Props componentName="SearchList" />
