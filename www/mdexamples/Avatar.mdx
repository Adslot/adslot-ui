import Props from '../containers/Props.jsx';
import DesignNotes from '../containers/DesignNotes.jsx';

## Avatar

```jsx live=true
const Example = () => (
  <>
    <Avatar givenName="James" surname="Smith" />
    <Avatar givenName="Daniel" surname="Radcliff" color="green" />
    <Avatar givenName="Ray" surname="Mysterio" color="blue" />
  </>
);

render(<div style={{ display: 'flex', width: 150, justifyContent: 'space-between' }}>{<Example />}</div>);
```

<DesignNotes>
  <div>
    <p>Avatars are used to personalise the user’s experience.</p>
    <p>
      <label>Example:</label> Profile area, Inbox and User section.
    </p>
  </div>
</DesignNotes>

<Props componentName="Avatar" />
