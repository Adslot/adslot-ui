import Props from '../containers/Props.jsx';
import DesignNotes from '../containers/DesignNotes.jsx';

## Date Picker

```jsx live={true}
const Example = () => {
  const [startDate, setStartDate] = React.useState(new Date());
  const handleChange = (startDate) => setStartDate(startDate);

  return (
    <DatePicker
      className="form-control"
      dateFormat="DD MMM YYYY"
      selected={startDate}
      onChange={handleChange}
      placeholderText="Date e.g. 03 Sep 2016"
      disableInlineEditing={false}
    />
  );
};

render(Example);
```

<DesignNotes>
  <div>
    <a href="https://github.com/Hacker0x01/react-datepicker">React DatePicker Documentation</a>
  </div>
  * For a Date Range Picker use two Date Pickers with minDate and maxDate props for validation
</DesignNotes>
<Props componentName="DatePicker" />
