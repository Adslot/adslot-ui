import Props from '../containers/Props.jsx';
import DesignNotes from '../containers/DesignNotes.jsx';

## Tile

```jsx live=true
const Example = () => {
  const items = [
    {
      title: '',
      imgLink: './assets/tile/adslot-logo-1.png',
    },
    {
      title: '',
      imgLink: './assets/tile/adslot-logo-2.png',
    },
    {
      title: '',
      imgLink: './assets/tile/logo-placeholder.png',
    },
    {
      title: 'Sport',
    },
    {
      title: (
        <div style={{ display: 'flex', alignItems: 'center', justifyContent: 'center', lineHeight: '16px' }}>
          <SvgSymbol href="./assets/svg-symbols.svg#calendar" />
          <div style={{ marginLeft: 5 }}>Calendar</div>
        </div>
      ),
    },
  ];

  return (
    <div>
      <React.Fragment>
        <Carousel autoplay={false} style={{ width: '55vw' }} dots={false}>
          {_.map(items, item => (
            <Tile {...item} style={{ paddingRight: 10, width: 220, height: 300 }} />
          ))}
        </Carousel>
      </React.Fragment>
    </div>
  );
};
render(Example);
```

<Props componentName="Tile" />
